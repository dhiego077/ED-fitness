# ED Fitness V12 — diagnóstico do catálogo

- Remove o fallback silencioso de produtos de teste nas páginas de categoria.
- Exibe erro real do Firestore quando a leitura falhar.
- Registra no Console os produtos ativos recebidos do Firestore.
- Continua aceitando `image` como imagem principal.
- Já reconhece `images` (array) e usa o primeiro item como capa, preparando a galeria.
- Nenhum deploy foi realizado.
